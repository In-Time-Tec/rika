# Reduced tool surface

**Gain:** local file search, content search, reads, and shell cover coding work without owning an embedding index or structural-outline subsystem. External providers may still supply semantic code research.

**Cost:** semantic code research depends on a configured external provider, and Rika does not offer ast-grep outlines.

**Rejected:** keeping dormant versions still adds dependencies, configuration, tests, and UI concepts.
