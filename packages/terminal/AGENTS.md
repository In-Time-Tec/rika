# @rika/terminal

Owns pure terminal state and the only OpenTUI adapter. It consumes product events and callbacks, never SQL, Generalist, or provider APIs. The terminal background is inherited and renderer lifecycle is scoped.
