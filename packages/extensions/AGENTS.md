# @rika/extensions

Owns local extension discovery and activation. It may depend on TenetKit public extension contracts but must not import TenetKit internals, application, runtime, TUI, tool, persistence, MCP, or plugin modules.
