# @rika/extensions

Owns local extension discovery and activation. It may depend on Generalist public extension contracts but must not import Generalist internals, application, runtime, TUI, tool, persistence, MCP, or plugin modules.
