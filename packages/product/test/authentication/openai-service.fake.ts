import { Service, layer } from "../../src/authentication/openai-service"
import * as Contract from "../../src/authentication/openai-contract"

export { Service, layer }
export const TokenResponse = Contract.TokenResponse
